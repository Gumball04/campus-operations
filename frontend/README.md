Frontend scaffold (Vite + React + Tailwind).

To run:

npm install
npm run dev

The UI is a placeholder; implement pages and styles as needed.
# Frontend (scaffold)

This folder will contain a React frontend.

Planned structure:

- `src/` - source code
- `public/` - public static assets
- `components/` - reusable UI components
- `pages/` - top-level page views
- `services/` - API service wrappers
- `hooks/` - custom React hooks
- `layouts/` - layout components
- `assets/` - images, icons, fonts

No implementation yet — this is a scaffold for future frontend work.
